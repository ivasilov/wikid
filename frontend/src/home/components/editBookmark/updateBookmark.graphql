mutation updateBookmark($params: UpdateBookmarkInput!) {
  updateBookmark(params: $params) {
    id
    url
    name
    read
    pages {
      id
      name
      description
      content
    }
  }
}
